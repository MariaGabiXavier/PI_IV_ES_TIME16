Para rodar servidor:

1° terminal:
    entre na pasta : 
        cd PI_IV_ES_TIME16\web\backend\adapter
    rode: 
        npm install
        npm start

2° terminal:
    entre na pasta:
        cd PI_IV_ES_TIME16\chatBot
    rode: 
        javac servidor\*.java cliente\*.java
        java servidor.Servidor

Para ver com o front, é so rodar normalmente o site, agora se quiser ver apenas o back do servidor cliente abra:
3°terminal:
    entre na pasta:
        cd PI_IV_ES_TIME16\chatBot
    rode: 
        java cliente.Cliente


Para inicial servidor com o MongoDB
1° terminal:
    entre na pasta : 
        cd PI_IV_ES_TIME16\web\backend
    rode: 
        npm install
        npm run dev


obs: terminais cmd 